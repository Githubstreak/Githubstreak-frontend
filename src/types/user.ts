export type User = {
  rank: number;
  developer: string;
  role: string;
  team: string;
  status: string;
  streak: string;
  contributions?: string;
  username?: string;
  avatar: string;
  email: string;
};
